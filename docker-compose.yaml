services:
  # gobang_app:
  #   container_name: gobang_app
  #   build:
  #     context: .
  #     dockerfile: docker/gobangApp/Dockerfile
  #   networks:
  #     - gobang_network
  #   ports:
  #     - ${APPPORT:-3000}:3000
  #   depends_on:
  #     - gobang_auth

  gobang_auth:
    container_name: gobang_auth
    build: .
    networks:
      - gobang_network
    ports:
      - "${PORT:-8080}:8080"
    environment:
      - PORT=${PORT:-8080}
      - DB_PORT=${DB_PORT:-3306}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME:-gobang}
    depends_on:
      - db
  # gobang_db:
  #   container_name: gobang_db
  #   image: mysql:latest
  #   networks:
  #     - gobang_network
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=${DB_PASSWORD}
  #     - MYSQL_DATABASE=${DB_NAME:-gobang}

  # volumes:
  #   gobang_db:
  #     name: gobang_db
  #     driver: local

networks:
  gobang:
    name: gobang_network
    driver: bridge
